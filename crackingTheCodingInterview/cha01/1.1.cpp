/* Implement an algorithm to determine if a string has all unique
   characters. What if you can not use additional data structures? */
/* i don't quite understand what *all unique characters* means */
#include <iostream>
#include <string>
#include <map>
using namespace std;

bool has_all_unique_characters(string s)
{
  map<char, int> cmap = map<char, int>();
  
  return true;
}

int main()
{
  return 0;
}